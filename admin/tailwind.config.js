/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      // Colors, fonts, and radii are defined via @theme in src/index.css (Tailwind v4).
    },
  },
  plugins: [],
}
